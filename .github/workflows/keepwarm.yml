name: Keep Warm

on:
  schedule:
    - cron: "*/10 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: curl --fail ${{ secrets.CLOUD_RUN_URL }}/api/v1/health
