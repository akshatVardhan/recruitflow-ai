# .claude/ is Claude Code-specific and not compatible with current tooling
.claude/

# Agent tooling/process docs - not secrets (no credentials live here, ever -
# that's Doppler-only), just kept off the public repo. Lives in the private
# companion repo recruitflow-ai-agents instead - run
# scripts/sync-agent-files.sh to pull it into place (needed once per clone
# and once per new worktree).
.agents/
AGENTS.md

# Environment variables - never commit
.env
.env.local
.env.*.local

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.venv/
venv/
*.so

# Node
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo

# OS
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Docker
*.log

# Uploads (local dev only)
uploads/
