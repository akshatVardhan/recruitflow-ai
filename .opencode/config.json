{
  "mcpServers": {
    "github": {
      "type": "github",
      "token": "$GITHUB_MCP_TOKEN"
    },
    "jira": {
      "type": "atlassian",
      "apiToken": "$JIRA_API_TOKEN",
      "email": "$JIRA_EMAIL",
      "baseUrl": "$JIRA_BASE_URL"
    }
  }
}
